# TASKS — warehouse_app

Custom app untuk org gudang (finished-goods warehouse). Keputusan FU57 (production_app, 2026-09-20): native ERPNext harus bebas dari pengaruh custom app manufacturing; production_app fokus user manufacturing; tooling gudang = app terpisah ini.

Planning fitur pertama: lihat `IMPLEMENTATION_PLAN.md` (keputusan user 2026-09-20 + rulings advisor R1-R10). Eksekusi per entri di bawah; satu entri = satu commit; DONE butuh bukti eksekusi.

## W1 — Scaffold + install di site frontend — DONE (2026-09-20)
- `bench new-app warehouse_app` di container backend (title "Warehouse App", license mit), lalu `bench --site frontend install-app warehouse_app` (site yang sama dengan production_app).
- Kode host = repo ini. Sinkron ke container via docker cp (folder `apps/` bukan bind mount) ke 6 container frappe: backend, frontend, queue-short, queue-long, scheduler, websocket + chown frappe:frappe.
- 4 container non-backend (queue×2, scheduler, websocket) juga perlu `production_app.pth` + `warehouse_app.pth` + dist-info di `env/lib/python3.14/site-packages` (image hanya pernah pip-install frappe+erpnext) — disalin dari backend.
- Bukti: `bench --site frontend list-apps` → warehouse_app 0.0.1 tercantum; `frappe.get_installed_apps` terbaca dari scheduler; `import warehouse_app` OK dari python worker queue (queue-short) versi 0.0.1; `ping` → pong; home 200.
- Heal utang lama (side effect W1): container queue/scheduler/websocket tidak pernah punya kode `production_app` (ModuleNotFoundError tiap boot worker sejak image dibangun). Kode + .pth production_app kini disalin ke 4 container itu. Sisa pre-existing, tidak diketuk: `pos_next`, `bakery_manufacturing`, `email_delivery_service` juga tidak ada di container queue (error non-fatal, worker tetap jalan).

## W2 — Verifikasi pradesain (Fase A) — DONE (2026-09-20)
Bukti eksekusi (docker exec / grep pada source terpasang), tercatat verbatim di `IMPLEMENTATION_PLAN.md` §4:
- `@frappe.whitelist()` di mapper `make_stock_entry` — `material_request.py:935-936`; qty = sisa pending; `batch_no` tidak ter-map.
- Over-qty ditolak atomik saat SE submit — `material_request.py:424-429`; MR "Transferred" saat `per_ordered==100` — `status_updater.py:125-127`; hook update — `erpnext/hooks.py:358-359`.
- Replacement trap Custom DocPerm — `frappe/permissions.py:512`; Workspace v16 punya `roles` — `frappe/desk/doctype/workspace/workspace.json`.
- Audit mirror production_app (R8): `handover.py:735-777` fail-safe, hanya `db.set_value("Work Order", …)` (`:723,:1249`) — field tracking native MR/SE tidak disentuh.

## W3 — Script Report "Serah Terima Gudang" (Fase B) — PENDING
Depends: W2. Files & desain: `IMPLEMENTATION_PLAN.md` §5 Fase B (grain MR Item, filter arah gudang, bucket `per_ordered`, qty-only, roles Gudang Barang Jadi + Stock Manager; butuh `__init__.py` folder modul `warehouse_app/warehouse_app/warehouse_app/` yang kini kosong).
Gate: migrate sukses; query via API sebagai fixture user role gudang; 3 kasus derivasi benar (belum/sebagian/terkirim) atas data uji yang lalu dibersihkan; user tanpa role ditolak.

## W4 — Aksi buat SE dari papan (Fase C) — PENDING
Depends: W3. `serah_terima_gudang.js`: chip status + tombol "Buat Stock Entry" → `frappe.model.open_mapped_doc` ke mapper native; fallback kolom link MR (2 klik) bila tombol in-report rapuh — putusan dicatat di bukti.
Gate: SE draft berisi sisa; submit native sukses; over-qty ditolak native; bucket papan berganti; nol endpoint baru.

## W5 — Workspace "Warehouse App" (Fase D) — PENDING
Depends: W3. `…/warehouse_app/workspace/gudang/gudang.json`: public + roles [Gudang Barang Jadi], shortcut report + list MR/SE + Stock Balance; content header sederhana.
Gate: workspace terlihat role gudang & System Manager, tak terlihat user lain (cek API desktop); tersinkron 6 container (pipeline deploy AGENTS.md).

## W6 — E2E walkthrough + residu 0 + guard perm (Fase E) — DONE (2026-09-20)
- Walkthrough browser end-to-end sebagai fixture user `zztest-w6@example.com` (Gudang Barang Jadi + Stock User, bukan akun asli): login → nav "Gudang" tampil → workspace "Gudang — Papan Serah Terima" (4 shortcut) → papan "Serah Terima Gudang" render data nyata produksi + fixture (chip Belum Dikirim/Sebagian/Terkirim, kolom WO & Box display-only, tanpa valuasi) → tombol "Buat Stock Entry" membuka SE draf via mapper native (Material Transfer, qty=sisa, gudang benar) → Save+Submit `MAT-STE-2026-07047` sukses → papan otomatis pindah bucket "Terkirim" & tombol hilang. Bukti screenshot: `/tmp/w6-screens/`.
- Temuan & perbaikan (commit `a070259`, hasil review independen + verifikasi eksekusi):
  1. Nav atas /desk dirender dari **Desktop Icon → Workspace Sidebar** — tanpa keduanya grup "Gudang" tidak muncul walau workspace diizinkan (`boot.workspace_sidebar_item` memuat, tapi render butuh ikon). Fix: json `workspace_sidebar/gudang/gudang.json` + `upgrade.py` idempoten (`ensure_workspace_sidebar` + `ensure_desktop_icon`, after_install/after_migrate) — pola `production_app.upgrade.ensure_desktop_icon`.
  2. Default filter Link harus nama eksak: "Gudang Barang Jadi" ditolak client ("Warehouse … not found"); diganti "Gudang Barang Jadi - ROPI" (`resolve_warehouse` tetap menerima bentuk tanpa abbr).
  3. Guard residu buta terhadap Item bernama auto (site memakai Item naming series — name/item_code dioverride; ITEM00248 "ZZTEST W3 Gate Item" lolos guard lama). Fix: hitung/sweep juga via `item_name` (prefix + varian spasi).
  4. File stray package-level `workspace_sidebar/gudang.json` (drift di container; AppleDouble dari docker cp macOS bisa menggugurkan migrate) dihapus dari 6 container; pipeline sync kini menyertakan pembersihan `._*`.
- Guard final pasca-teardown `ok: true` — residu ZZTEST-W semuanya 0; Custom DocPerm Company = 12 baris baseline identik (nol dari warehouse_app); Stock User read Company = 1. 6 container sinkron + restart + ping pong.
- Catatan rough edge native (pre-existing, tidak memblokir, tidak di-fix di sini): toast "No permission for Stock Settings" saat user non-admin membuka form Stock Entry di Desk.

## W7 — Company read utk Gudang Barang Jadi — DITUTUP (2026-09-20, keputusan user)
User memutuskan: **user gudang dipasangkan role `Stock User`** (SOP, nol kode — Stock User memang punya read Company, `company.json:1078-1080`). Tidak akan dieksekusi sebagai kode; entri disimpan untuk riwayat. Bila kelak kebijakan berubah ke single-role, desain fallback (`frappe.permissions.add_permission` additive-only) tetap ada di `IMPLEMENTATION_PLAN.md` §7.

## W8 — Smoke test endpoint dormant production_app (PARKIR, non-W2)
Trigger: kapan pun setelah W6. Panggil `create_request`/`cancel_request`/`fulfill_form_order` (production_app) dengan fixture, pastikan tak membusuk, bersihkan residu.

## W9 — Request serah terima ala-gudang (adonan ke + item) — DONE (2026-09-20)
Depends: fitur pertama live (W3–W6). Keputusan user: gudang butuh bikin request per kanban/WO dalam bahasa mereka (adonan ke + nama item), bukan nomor WO + qty; implementasi hanya di warehouse_app, tanpa sentuh core/native. Ruling advisor **R11** (Opsi A): UI tipis (Page `gudang_request`: daftar WO "Adonan {ke} — {item}" + dialog Box 1/2 + tombol Cancel) memanggil `production_app.api.handover.create_request/cancel_request` dari client (endpoint tetap satu penulis ringkasan WO); picker = endpoint warehouse_app sendiri (`warehouse_app.warehouse_app.gudang_request.requestable_work_orders`, `frappe.get_list` Work Order — gudang punya read WO, tanpa bypass perm); papan Serah Terima Gudang ditambah kolom "Adonan" (left join WO via `custom_work_order`, display-only, diposisikan sebelum kolom WO); validasi box andalkan server. Konfigurasi site: Manufacturing Settings `custom_default_handover_warehouse` = "Gudang Barang Jadi - ROPI" (sebelumnya kosong).
- Gate `warehouse_app.tests.w9_gate.run_gate` → `ok:true` (16 checks): negative perm (picker & create ditolak untuk user tanpa role), picker menampilkan WO fixture dengan adonan + item_name + flag `request_active`, search adonan/item, `create_request` sebagai user gudang → MR submitted Material Transfer + row `custom_work_order` + ringkasan WO terisi, guard duplikat production_app menolak request kedua, papan menampilkan MR fixture (kolom Adonan idx-2, bucket Belum Dikirim), `cancel_request` → MR batal + ringkasan WO bersih, teardown residu 0. Guard umum `ok:true` (residu ZZTEST-W = 0; Custom DocPerm Company = baseline 12 baris).
- Temuan eksekusi: (1) Page desk butuh nama underscore murni — `scrub()` mengubah dash→underscore sehingga `load_assets` mencari folder/file `gudang_request`; page dinamai `gudang_request` (folder `page/gudang_request/`, file `gudang_request.js|css|json`), rute `/app/gudang_request`. (2) Setting serah terima production_app kosong di site ini — diset sekali sebagai konfigurasi (tercatat di R11), bukan kode warehouse_app. (3) Fixture WO/SE Manufacture: WO `ignore_validate` + docstatus manual; SE Manufacture butuh `expense_account` + `cost_center` di row FG.
- Review independen: APPROVE — 0 P0/P1; fix P2 diterapkan (normalisasi `search` non-string di picker; `on_page_show` reload agar badge "Diminta" tidak basi). 6 container sinkron + restart + ping pong; shortcut pertama workspace "Gudang" = "Request Serah Terima".
- **Revisi umpan balik user (2026-09-20, commit kedua)**: kartu diganti **tabel checklist full-width** — kolom Adonan/Item/Hasil/WO/Status sejajar, checkbox per baris + pilih-semua (indeterminate), baris klik-untuk-centang, baris "Diminta" non-selectable dengan tombol Batalkan, dan SATU aksi **"Buat Request (N)"** untuk 1..N WO (dialog alokasi Box per baris, jumlah prefilled = hasil WO; submit sekuensial dengan ringkasan sukses/gagal per WO). Panel kepadatan (Nyaman/Padat, persist di localStorage) di pojok kanan-bawah. Temuan eksekusi: `.layout-main` Desk v16 = flex row — konten page wajib dibungkus satu root full-width (ini juga penyebab tampilan kartu lama berantakan); `frappe.ui.keyCode` tidak ada di v16 (Enter search pakai `e.key`); dialog konfirmasi terbukti via browser fixture-user (login → tabel → multi-select → dialog → validasi kg kosong → search), lalu logout + purge sesi/user fixture. Gate + guard tetap `ok:true` pasca revisi.
- **Revisi kedua (2026-09-20, permintaan user "filter kayak erpnext bawaan")**: tombol **Filter** dengan panel baris `[Field][operator][Nilai]` + Tambah Filter/Hapus Semua (komponen legacy `frappe.ui.FilterGroup` sudah tidak ada di v16 — desk baru Vue — jadi UX-nya ditiru). Field whitelist server (`filter_fields` + validasi `_parse_filters`): Adonan (=,≠), Item (nama/kode, seperti/=), Status WO (=,≠, opsi dari meta), No. WO, Hasil (≥,≤,=), Gudang Hasil; field "Item" virtual di-expand ke `production_item in (...)` via pencarian nama+kode — get_list tetap parameterized + perm ditegakkan. Filter digabung AND dengan pencarian; auto-apply terdebang 300ms; badge jumlah filter aktif. Temuan eksekusi: `stopPropagation` pada popover membunuh handler delegated `.layout-main` (tambah/ubah filter tak jalan) — diganti tutup-popover via `closest()` di handler document. Gate +2 cek (`picker_filter`, `picker_filter_whitelist`) → `ok:true` 20 checks; browser fixture-user: kombinasi Adonan=34 + Item~Krim tepat, hapus-semua pulih, multi-select tetap jalan; residu di-purge, guard `ok:true`.

## W10 — Rapikan tampilan gudang_request ala ERPNext + label Inggris — DONE (2026-09-20)
Keputusan user: tampilan harus **ala List View ERPNext native** (tabel polos tanpa card, warna hanya variabel tema, pill indicator bawaan) dan **seluruh label UI Bahasa Inggris** (istilah domain "Adonan" → "Batch"). Lingkup: `page/gudang_request/gudang_request.{js,css,json}` + label di `gudang_request.py` — nol perubahan endpoint/logika kecuali dua fix di bawah.
- Perubahan UI: judul Page + breadcrumb "Handover Requests"; semua string UI EN (toolbar, tabel, filter, dialog, konfirmasi, alert, operator like/not like); header tabel non-uppercase 12px ala native; kolom Item 600; pill `indicator-pill blue` untuk status WO & `orange` untuk "Requested · MR" dengan MR sebagai link ke form; tombol bulk setinggi tombol toolbar; dialog alokasi header terkelompok Box 1/Box 2 (colspan/rowspan) + pemisah vertikal antar grup + input 72px; empty state center; `:focus-visible` outline primary; tanpa warna/font custom.
- **Temuan & fix (1) — sticky header memicu mispaint**: `thead th { position: sticky; top: 49px }` membuat baris data tidak tercat pada re-render (terverifikasi browser fixture: DOM benar — 1 baris, hit-test tepat — tetapi raster mengecat header di posisi baris). Fix dengan penghapusan: sticky dibuang, header ikut scroll ala list view native; render kini benar pada pencarian/filter. Konstanta magic 49px juga hilang.
- **Temuan & fix (2) — bug fungsional pre-existing W9**: operator `like` diteruskan tanpa wildcard ke get_list (Frappe tidak menambah `%`), sehingga filter "Work Order No. like 00236" selalu kosong (W9 dulu hanya menguji `=` Adonan dan `like` Item yang memang sudah dibungkus `%` di `_item_filter`). Fix satu tempat di `_parse_filters`: nilai `like` dibungkus `%value%`.
- Judul Page di DB diset "Handover Requests" (selaras json; Page title cached di DB).
- **Revisi spacing (umpan balik user "mepet semua")**: root full-bleed tanpa padding membuat konten menempel ke sidebar/tepi viewport (tombol Create Request terpotong). Fix: `.wzrq-root` diberi `padding 16px 32px 56px` + `max-width: 1600px` center; toolbar gap/margin dinaikkan; padding sel tabel `12px 14px` (compact `6px 14px`); kolom check 44px. Terverifikasi browser pada viewport 1920px, light + **dark mode** (`data-theme=dark`, vars flip benar) — guard `ok:true` pasca purge fixture ulang.
- **Revisi popover filter (umpan balik user)**: panel dikompakkan — lebar 560px, padding `8px 14px 12px`, latar `--popover-bg`; kontrol filter rata 28px / font 13px; tombol × 28px muted; state kosong "No filters" 12px muted; footer link 12px medium muted (hover: teks normal / merah untuk Clear All). Terverifikasi dark mode (empty state + baris filter aktif), guard `ok:true`.
- **Revisi fungsi (umpan balik user: "filter tanggal, Yield→Qty, kolom bisa diganti")**: (1) filter **Date** (field `creation`, operator `between`/`≥`/`≤`) — UI `between` = dua input `type=date` berdampingan; server `_date_filter` memperluas batas hari ke 00:00–23:59 karena `creation` bertipe datetime (satu sisi kosong → otomatis jadi `>=`/`<=`); (2) header kolom "Yield" → **"Qty"** (tabel + label filter + copy dialog); (3) **pemilih kolom** ala list view ERPNext — ikon sliders di pojok kanan header membuka popover "Choose columns" (Batch/Item/Qty/Work Order/Warehouse/Status), show/hide tersimpan di `localStorage.wzrq_columns`, render ulang tanpa fetch; kolom baru **Warehouse** (`fg_warehouse`, muted). Tabel kini dirender dinamis dari definisi kolom (thead+tbody).
- Bukti tambahan revisi fungsi: gate W9 `ok:true` (20 checks) pasca perubahan server; browser fixture-user dark mode: toggle Batch off → kolom hilang + tersimpan + bertahan setelah reload; filter Date 19–20 Sep → 0 baris (benar, WO terakhir 16 Sep) dan 14–16 Sep → 8 baris.
- **Revisi dialog Box Allocation (umpan balik user "ramai & membingungkan")**: header dua lapis (grup Box + kg/qty) diganti satu baris (`Work Order | Box 1 · kg | Box 1 · qty | Box 2 · optional`); input Box 2 disembunyikan di balik tombol `+ Box 2` per baris (baris default hanya 2 input; klik memunculkan pasangan kg/qty + fokus, baris tersembunyi dikirim sebagai box_2=0); paragraf penjelasan dipangkas jadi satu kalimat ("Box 1 is required. Box 2 is optional…"); identitas baris dua tingkat rapi (judul tebal tanpa wrap + meta 12px nowrap). Logika/validasi server tak berubah — Box 1 tetap wajib, Box 2 tetap opsional. Terverifikasi browser dark mode (dialog + reveal Box 2), ditutup tanpa submit; guard `ok:true`, residu 0.
- **Bug fix (umpan balik user "create request ga bisa, tetep sama aja")**: klik Create Request di dialog crash diam-diam — `frappe.freeze is not a function` (API legacy sudah dihapus di Desk v16; async function → unhandled rejection, tanpa pesan apa pun). Fix: freeze overlay diganti men-disable tombol primer selama submit (anti dobel-klik); error `frappe.call` tetap ditangani msgprint seperti semula. Terverifikasi browser fixture: submit dengan qty sengaja salah → call terkirim, server menolak dengan pesan jelas ("Jumlah Pack … harus tepat 96 Pack") → msgprint "All failed" tampil, dialog tertutup, **nol MR dibuat**; tanpa error JS. Container sempat mati total (host restart) — dipulihkan via `docker compose -f /Users/rotiropi/erpnext-new/pwd.yml up -d`.

## W11 — Settings source/target warehouse + sidebar gudang — DONE (2026-09-20)
Keputusan user: (1) perlu pengaturan source/target warehouse, (2) sidebar dirapikan, sidebar sendiri untuk gudang.
- **Halaman `gudang_settings` (Page "Settings")**: form dua field — Source Warehouse (kosong = ikut gudang asal lot produksi, R2) dan Target Warehouse (wajib untuk create) — yang membaca/menulis dua field Manufacturing Settings milik production_app (`custom_default_handover_source_warehouse` / `custom_default_handover_warehouse`). production_app tetap satu penulis alur (R11): tanpa perubahan endpoint/signature; set dibatasi role System Manager / Gudang Barang Jadi (`_require_set_access`), validasi Warehouse non-group. Page roles = sama dengan gudang_request.
- **Pemulihan insiden**: `bench migrate` (saat apps.txt cuma berisi erpnext+frappe) menjalankan rekonsiliasi app → `installed_apps` global terpangkas → production_app/warehouse_app/pos_next keluar dari module map → SEMUA Page/Report app lain gagal "Module X not found" via web. Fix: apps.txt dilengkapi 5 app, `installed_apps` global dipulihkan (db.set_global), kedua cache modul dihapus (`app_modules` di frappe.cache + `installed_app_modules` di frappe.client_cache), restart. Pelajaran: web request memakai `setup_module_map(include_all_apps=False)` = client cache `installed_app_modules`; CLI memakai global `app_modules` — dua-duanya harus bersih.
- **Sidebar/workspace**: shortcut "Request Serah Terima" diganti "Handover Requests" (selaras rename), ditambah shortcut + kartu **Settings**, urutan: Handover Requests → Serah Terima Gudang → Settings → MR → SE → Stock Balance. Workspace role-scoped Gudang Barang Jadi — user gudang murni hanya melihat grup Gudang ini.
- **Bug fix prefill qty dialog**: dialog memprefill `produced_qty` (Pcs, 1.152) padahal server memvalidasi dalam display UOM (Pack, 96 = /12) → create selalu ditolak. Picker kini menghitung `expected_units` memakai `_enrich_units` production_app (sumber konversi yang sama dengan validasi — T39, tidak diduplikasi; fallback stock qty bila import gagal), dialog prefill `expected_units` + meta "qty in Pack · yield 1,152 Pcs".
- Bukti: gate W9 `ok:true` (menangkap UnboundLocalError `flt` shadow saat draft pertama — diperbaiki lalu lolos 20 checks); guard `ok:true` residu 0; browser fixture-user dark mode: Settings page tampil nilai db (target=Gudang Barang Jadi - ROPI, source kosong) + Save no-op sukses; dialog prefill 96; workspace cards rapi. 6 container sinkron + ping.
- Bukti eksekusi (browser sebagai fixture user `zztest-w10-ui@example.com`, Gudang Barang Jadi + Stock User, login via API — bukan akun asli): tabel 50 baris ala list view native (kolom Batch/Item/Yield/Work Order/Status, pill biru "Completed"); popover filter English (Batch, Work Order No., like, + Add Filter, Clear All Filters); pencarian ketik "00236" → tepat 1 baris dengan pill orange "Requested · MAT-MR-2026-00269" (link) + checkbox disabled + tombol Cancel; dialog "Box Allocation — 2 Work Orders" dengan header Box 1/Box 2 terkelompok, qty prefill 26/25, pemisah vertikal antar grup; dialog ditutup tanpa submit — nol MR dibuat. Guard `ok:true` (residu ZZTEST-W = 0 termasuk User/Sessions/Activity Log; DocPerm Company baseline 12 baris). 6 container sinkron + restart + ping pong.

## W12 — Qty per UOM pilihan user + kolom tabel + opsi Pack/Pcs dialog — DONE (2026-09-20)
Keputusan user: qty harus bisa dilihat/diisi dalam Pcs atau Pack (pilihan user, persist per browser), dan kolom tabel dilengkapi ala list view Stock Entry. Nol perubahan endpoint/kontrak — `production_app.api.handover.create_request` tetap menerima qty dalam display UOM integer; konversi dilakukan client sebelum kirim.
- **Qty tabel per UOM**: toolbar dapat select `.wzrq-uom` (kluster kanan, dekat grup bulk; persist `wzrq_qty_uom`, default stock uom, nilai tersimpan tak ada di opsi → pakai default). Opsi = union stock+display uom baris termuat (stock duluan, dedup), disegarkan tiap render; tanpa baris opsi terakhir dipertahankan. Kolom Qty per baris: terpilih = display uom baris → expected_units + display uom; = stock uom → produced_qty + stock uom; factor tak valid/uom tak dikenal → fallback stock. Header dinamis `Qty (Pcs)` / `Qty (Pack)`; ganti opsi = re-render tanpa fetch.
- **Kolom baru pemilih kolom**: `Item Code` (production_item, muted nowrap) + `Created` (`frappe.datetime.str_to_user(creation)`, muted nowrap) — popover Choose columns memuatnya otomatis; server get_list kini mengambil `creation` (satu-satunya perubahan py). Preferensi `wzrq_columns` lama tetap valid — kolom baru tersembunyi bagi pemakai yang sudah punya preferensi tersimpan (acceptable).
- **Dialog Box Allocation Pack/Pcs**: baris hint jadi flex — kiri hint, kanan kontrol `Qty in [select .wzrq-duom]` (28px/13px ala kontrol panel filter; persist `wzrq_dialog_uom`, default display uom baris pertama). Prefill qty1 per mode (display → expected_units; stock → produced_qty dibulatkan); ganti mode = KONVERSI nilai yang sudah diinput per baris (display = stock/factor, stock = display×factor; maks 3 desimal tanpa nol ekor; input kosong dibiarkan kosong — tidak reset kerja user). Meta baris menampilkan kedua uom: `MFG-WO-… · yield 1,152 Pcs · 96 Pack` (bagian display disembunyikan bila sama dengan stock). Submit: mode display → parseInt seperti kini; mode stock → qty/factor, bila tak bulat (|x−round(x)| > 1e-6) tampilkan msgprint merah English "… is not a whole number of Packs …" dan ABORT seluruh pengiriman (nol request terkirim); kg tak berubah; payload API tetap bentuk lama (integer display UOM). Factor/item per baris dibawa sebagai atribut `data-*` di tr dialog.
- Eksekusi via workflow (`/workflow`): implementor → reviewer → fix → sinkron 6 container → gate. Review independen menemukan 1 regresi nyata (P2): re-render tanpa fetch (toggle uom / pemilih kolom) tidak mengosongkan Set `selected` closure → counter "3 selected" & dialog bisa memuat WO yang checkboxnya hilang; di-fix di kedua jalur (`selected.clear()` sebelum `wzrq_render`, pola bulk-clear) — jalur pemilih kolom yang identik pre-existing ikut diperbaiki.
- **Insiden gate W9 & fix environment-aware**: gate gagal di `create_request` ("tidak punya lot tersedia") BUKAN karena W12 — user asli mengisi source warehouse serah terima = "Cold Storage Produksi - ROPI" via halaman Settings W11 (21:19:50), sehingga pool lot production_app (R2: setting override) berpindah dari asumsi lama gate "Gudang Produksi". Fix fixture `w9_gate.py` (ruling escalation): pool = `custom_default_handover_source_warehouse` (fallback Gudang Produksi bila kosong), SE fixture + assertion `set_from_warehouse` mengikuti pool; setting menunjuk warehouse tak valid/non-group → gate gagal jujur di preflight (tidak di-skip, nol assertion dihapus). Setting live user TIDAK diubah (konfigurasi deliberate). Gate kini `ok:true` 20 checks + guard `ok:true` di environment terkonfigurasi.
- Bukti eksekusi (browser fixture user `zztest-w12-ui@example.com`, login via API): tabel 50 baris, select UOM toolbar (opsi Pcs/Pack, default Pcs); toggle ke Pack → sel "1,176 Pcs"→"98 Pack" + header `Qty (Pack)`, persist `wzrq_qty_uom` & bertahan setelah reload; pemilih kolom memuat 8 kolom (Batch/Item/**Item Code**/Qty/WO/Warehouse/**Created**/Status), toggle Batch off→on bekerja + persist (Item Code "PJ260013", Created "20-09-2026 21:03:56" tampil); dialog "Box Allocation — 1 Work Orders": `.wzrq-duom` default Pack, prefill qty1=96, meta "MFG-WO-2026-03409 · yield 1,152 Pcs · 96 Pack"; konversi dua arah 96↔1,152 (100 Pcs → 8.333 Pack); submit 100 Pcs + kg 23 → msgprint merah "100 Pcs is not a whole number of Packs for Dough Ropi Coklat (1 Pack = 12 Pcs)", dialog tetap terbuka, **nol MR dibuat**. QA dark mode via computed style: vars flip benar (bg 23/35, teks 248/199), select 32px sejajar kluster kanan, nol overflow horizontal. Fixture di-purge (logout fetch 400 — quirk dikenal; purge via bench), guard `ok:true` residu 0.
- Sinkron: page + `gudang_request.py` via workflow; folder `tests/` (w9_gate.py baru) disinkron manual ke 6 container (workflow hanya menyalin page+py).

## W13 — Frappe Cloud readiness (role fixture + uji fresh install) — DONE (2026-09-20)
Permintaan user: commit & push, pastikan app bisa di-install di Frappe Cloud. Uji simulasi FC di container backend: `bench new-site` site baru dengan **erpnext + warehouse_app saja (tanpa production_app)**.
- Temuan 1 (installability): fresh install **lolos** — semua artefak ter-sync (Page gudang_request/gudang_settings, Workspace Gudang, Workspace Sidebar, Desktop Icon via `upgrade.apply`, Report Serah Terima Gudang), picker `requestable_work_orders` jalan (fallback `_enrich_units` — tanpa production_app, rows 0 di site kosong). Nol dependensi install-time ke production_app (apps tetap saling lepas).
- Temuan 2 (gap): role **"Gudang Barang Jadi" tidak ter-install** di site baru (workspace + Page role-scoped ke role ini) → admin FC harus buat manual. Fix: fixture `warehouse_app/fixtures/role.json` (wajib menyertakan `"name"` — fixture tanpa name menyebabkan `KeyError: 'name'` di sync fixture; tertangkap di uji install kedua) + deklarasi `fixtures` di hooks + `ensure_role()` idempoten di `upgrade.apply` (after_install & after_migrate).
- Bukti dua jalur: (1) **migrate** site uji lama tanpa role → `ROLE_AFTER_MIGRATE True`; (2) **fresh install** site ketiga dengan fixture terpasang → `V3_ROLE True` + semua artefak + picker OK, nol KeyError. Kedua site uji di-drop (sites/ kembali hanya `frontend`); guard site frontend `ok:true` pasca perubahan.
- Kebersihan repo: `__pycache__/*.pyc` yang ter-track dihapus dari index (gitignore sudah benar). metadata pyproject = scaffold bawaan bench v16 (flit_core, requires-python >=3.14 — sama dengan image frappe/erpnext:v16 yang jalan di site ini).

## W14 — Status "Shipped" utk WO yang sudah dikirim ke barang jadi — DONE (2026-09-20)
Umpan balik user: setelah SE diserahkan, baris WO kehilangan label MR dan tampil sama seperti WO yang belum pernah diminta (pill biru status) — operator bingung. Akar: `request_active=False` saat `mr in mr_dikirim`, dan tidak ada state ketiga (field `custom_handover_material_request` TIDAK dihapus production_app saat terkirim — terbukti DB: MR docstatus 1 "Transferred" + SED submitted).
- Fix: picker menambah `request_shipped` (MR submitted + ada SE submitted); UI kolom Status state ketiga — pill hijau **"Shipped · MAT-MR-… "** (link ke MR, `indicator-pill green`) — baris non-selectable (checkbox disabled + guard klik/check-all/update_bulk memakai `!request_active && !request_shipped`), tombol Cancel tetap hanya untuk "Requested".
- Bukti: DB 5 WO terkirim (MAT-MR-2026-00359/00358/00272/00336/00271) → browser fixture user: 5 pill hijau dengan link MR + checkbox disabled; gate W9 `ok:true` 20 checks + guard `ok:true` residu 0 pasca purge fixture.
- Temuan eksekusi: (1) sinkron W14 pertama gagal senyap — `SRC` host kurang satu tingkat `warehouse_app/` dan rantai `&&…; chown` menelan kegagalan cp (output "OK" palsu); pipeline sync wajib verifikasi `grep` di container. (2) bench console IPython: blok indentasi di baris terakhir file tak dieksekusi tanpa baris kosong penutup.

## W15 — Logo & branding app (apps screen + ikon sidebar/desktop) — DONE (2026-09-21)
Permintaan user: kasih logo custom app biar tidak polosan di ERPNext production.
- **Logo**: `public/logo.svg` (mark gudang biru #1B7BD5) + `logo-icon.svg`; ter-serve di `/assets/warehouse_app/logo.svg` **tanpa build** — symlink `sites/assets/warehouse_app` ternyata hanya ada di backend; dibuat manual di frontend container (penyebab 404 pertama).
- **Apps screen**: `add_to_apps_screen` diaktifkan di hooks (logo svg, title "Warehouse App", route `/app/gudang`, tanpa `has_permission` → semua user desk) — `frappe.get_hooks` kini memuat 4 app (frappe/erpnext/production_app/warehouse_app). Catatan: di site ini `/apps` 301 ke `/desk` (Desktop v16); branding apps screen berlaku di Frappe Cloud.
- **Ikon sidebar/desktop v16 = NAMA icon (feather/es-set), bukan SVG string**: kolom `Workspace.icon` hanya 140 char (SVG 218 → `Data too long`); `Workspace Sidebar.header_icon` bertipe `Icon`; item sidebar dirender dari `Workspace Sidebar Item.icon` (fallback "list"). Jadi: `Workspace.icon` = "package", `Workspace Sidebar.header_icon` = "package", `Workspace Sidebar Item` (parent Gudang) icon = "package" (DB) — konsisten dengan Desktop Icon "package" (W6). `upgrade.ensure_workspace_sidebar` kini membawa `header_icon` untuk site baru.
- Bukti: `/assets/warehouse_app/logo.svg` HTTP 200; browser fixture: sidebar item Gudang merender svg feather **package** (`item-icon="package"`); screenshot tersimpan; guard `ok:true` + gate W9 `ok:true` pasca purge fixture.
- Pelajaran pipeline: cp workspace json butuh path TIGA tingkat (`warehouse_app/warehouse_app/warehouse_app/workspace/…`) — dua kali salah tingkat di sesi ini; selalu verifikasi grep di container setelah cp.
