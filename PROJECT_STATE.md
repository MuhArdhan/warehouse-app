# PROJECT_STATE — warehouse_app

Head: 2026-09-21 — **W16 DONE**: sidebar sendiri live — Workspace Sidebar "Gudang" berisi 4 item nav (Gudang/Handover Requests/Serah Terima Gudang/Settings, icon lucide `package`/`clipboard-list`/`truck`/`settings`), sinkron idempoten via `upgrade.ensure_workspace_sidebar` (migrate tak men-sync JSON), active highlight otomatis; sidebar persisten di halaman custom gudang (terverifikasi browser fixture + dark mode; gate `w16_gate` ok:true 9 checks, residu 0). Pelajaran besar: cache redis per-email (doc cache global User, hash `roles`, `has_role:*` TTL 6 jam) bisa membusukkan render lintas run gate → email fixture wajib unik per run; item Report di-boot butuh role terdaftar + lolos `get_list("Report", user)`; ikon v16 = nama lucide (`icon-<name>`). **W15 DONE**: logo app (`public/logo.svg`) + `add_to_apps_screen` aktif (branding apps screen Frappe Cloud) + ikon v16 "package" konsisten di Workspace/Workspace Sidebar (+Item)/Desktop Icon (ikon v16 = nama icon, bukan SVG; header_icon dibawa upgrade.py). Symlink assets frontend container dibuat manual. **W14 DONE**: WO yang sudah dikirim ke barang jadi kini bertanda pill hijau **"Shipped · MAT-MR-…"** (link MR, non-selectable) — tidak lagi tampil seperti WO yang belum pernah diminta (umpan balik user; state ketiga di picker `request_shipped`). **W13 DONE (FC readiness)**: app teruji **fresh-install ala Frappe Cloud** (new-site erpnext+warehouse_app tanpa production_app — nol dependensi install-time; role "Gudang Barang Jadi" kini dibawa fixture + `ensure_role()` idempoten, terbukti di jalur install & migrate; site uji di-drop, guard ok) + push ke origin GitHub. **W12 DONE**: qty tabel & dialog per UOM pilihan user (Pcs/Pack, persist localStorage), dua kolom tabel baru (Item Code, Created) di pemilih kolom, dialog Box Allocation mode qty Pack/Pcs dengan konversi + abort bila Pcs tak membentuk Pack bulat (payload API tetap integer display UOM — kontrak production_app tak disentuh). Gate W9 kini **environment-aware** (pool source = setting `custom_default_handover_source_warehouse`, fallback Gudang Produksi — user asli sudah mengisi Cold Storage Produksi via Settings W11; setting tidak pernah diubah test). W11 DONE: halaman **Settings** gudang (source/target warehouse serah terima → Manufacturing Settings milik production_app, tanpa ubah endpoint), workspace/sidebar gudang dirapikan (label EN + shortcut Settings), dan fix **prefill qty dialog** (expected units dalam display UOM, mis. 96 Pack — bukan Pcs) + insiden pemulihan `installed_apps` terpangkas oleh migrate saat apps.txt tidak lengkap (rincian `TASKS.md` W11). W10 DONE: tampilan `gudang_request` ala List View ERPNext + label Inggris (+ fix sticky-header mispaint, filter `like` tanpa wildcard, freeze v16). W9 DONE: request serah terima ala-gudang live. W2–W6 DONE: papan serah terima live. Semua: residu fixture 0 (guard ok), 6 container sinkron + ping. Rulings R1–R11 + eksekusi tercatat di `IMPLEMENTATION_PLAN.md`/`TASKS.md`.
Next: pemakaian nyata user gudang; kandidat berikutnya (planning terpisah): opname/counting, MR gudang ke produksi. **Penting operasional**: start container selalu `docker compose -f /Users/rotiropi/erpnext-new/pwd.yml up -d`; `sites/apps.txt` wajib memuat semua app sebelum `bench migrate` (lihat insiden W11).
Keputusan user 2026-09-20: user gudang dipasangkan role `Stock User` — W7 ditutup tanpa kode. W8 (smoke test endpoint dormant production_app) masih PARKIR.

W10 — **DONE** (2026-09-20, keputusan user): tampilan page `gudang_request` dirapikan **ala List View ERPNext native** + **seluruh label UI Bahasa Inggris** ("Adonan" → "Batch"). Dua temuan penting: (1) sticky header `top:49px` memicu mispaint baris pada re-render — dihapus (header ikut scroll ala native); (2) bug fungsional pre-existing W9 — operator `like` tanpa wildcard membuat filter Work Order No./Gudang Hasil selalu kosong; di-fix di `_parse_filters` (`%value%`). Terverifikasi browser fixture-user (tabel, filter, baris "Requested" pill orange + link MR, dialog Box Allocation), guard `ok:true` residu 0, 6 container sinkron + ping. Rincian di `TASKS.md` W10.

W9 — **DONE** (2026-09-20): request serah terima ala-gudang (bahasa: adonan ke + nama item, bukan nomor WO). Ruling advisor **R11** (Opsi A, tercatat di IMPLEMENTATION_PLAN.md §3): Page `gudang_request` (rute `/app/gudang_request`, shortcut pertama workspace "Gudang") + picker read-only `warehouse_app.warehouse_app.gudang_request.requestable_work_orders` memanggil endpoint production_app `create_request`/`cancel_request` dari client — production_app tetap satu penulis ringkasan WO, endpoint dormant kini **dikontrak konsumen luar** (jangan dihapus/ubah signature tanpa koordinasi). Papan "Serah Terima Gudang" kini punya kolom **Adonan** sebelum Work Order. Konfigurasi site: Manufacturing Settings `custom_default_handover_warehouse` = "Gudang Barang Jadi - ROPI" (dulu kosong). Gate W9 `ok:true` (16 checks) + guard `ok:true` (residu 0, DocPerm baseline utuh) + review independen APPROVE; 6 container sinkron + ping pong. **Revisi UI atas umpan balik user**: halaman request kini **tabel checklist** (pilih 1..N WO, satu tombol "Buat Request (N)" dengan dialog alokasi Box per baris; baris "Diminta" non-selectable + tombol Batalkan; panel kepadatan) — terverifikasi browser fixture-user end-to-end tanpa MR nyata; sesi & user fixture di-purge.
